# 🚨 URGENT FIX - Backend 502 Error

## Проблема
Backend на Railway возвращает 502 error и не запускается из-за отсутствия переменных окружения.

## Решение

Откройте Railway Dashboard и добавьте следующие переменные для **backend** сервиса:

### Обязательные переменные:

```env
WEBAPP_URL=https://frontend-production-cd24.up.railway.app
FRONTEND_URL=https://frontend-production-cd24.up.railway.app
ADMIN_CHAT_ID=-1003380571535
```

### Шаги:

1. Откройте https://railway.app/
2. Найдите проект **sport-transformation**
3. Выберите сервис **backend** (или backend-production)
4. Перейдите на вкладку **Variables**
5. Добавьте три переменные выше:
   - Нажмите **+ New Variable**
   - Введите Name и Value
   - Нажмите **Add**
6. После добавления всех трёх нажмите **Deploy** или дождитесь автоматического редеплоя

### Проверка

После редеплоя проверьте:
```bash
curl https://sport-transformation-app-production.up.railway.app/health
```

Должен вернуть `{"status":"ok"}` вместо 502 error.

## Почему это произошло?

Backend пытается установить Telegram Menu Button с URL из `WEBAPP_URL`, но эта переменная не установлена, поэтому используется значение по умолчанию `http://localhost:5173`, которое Telegram отклоняет (требуется HTTPS). Это приводит к ошибке при запуске.

## Альтернативное решение (если нет доступа к Railway)

Если нет доступа к Railway Dashboard, можно временно откатить на предыдущую версию без nutrition:

```bash
git checkout main
git revert --no-commit HEAD~10..HEAD
git commit -m "temp: Revert to working version"
git push origin main
```

Но это удалит все изменения nutrition, поэтому **лучше добавить переменные в Railway**.
